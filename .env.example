# EmailJS — https://dashboard.emailjs.com
# These are client-side values and safe to expose publicly.
NEXT_PUBLIC_EMAILJS_SERVICE_ID=
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=

# The contact form posts these field names, so the EmailJS template
# must reference {{from_name}}, {{reply_to}} and {{message}}.
